/// <reference types="cypress" />
var registerQuery = require('./utils').registerQuery;
registerQuery('invokeFirst', function (methodName) {
    var args = [];
    for (var _i = 1; _i < arguments.length; _i++) {
        args[_i - 1] = arguments[_i];
    }
    var message = methodName;
    if (args.length) {
        message += ' ' + args.map(function (x) { return JSON.stringify(x); }).join(', ');
    }
    var log = Cypress.log({ name: 'invokeFirst', message: message });
    return function ($el) {
        var first = $el[0];
        return first[methodName].apply(first, args);
    };
});
